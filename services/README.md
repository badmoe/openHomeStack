# Services

Docker Compose definitions for all available services.

Each service has:
- `docker-compose.yml` - Container definition
- `config/` - Default configuration files (if needed)
- `README.md` - Service-specific documentation
